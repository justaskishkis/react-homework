value-count-4($values)
	$newValues = ();
	if length($values) == 1
		push($newValues, $values[0], $values[0], $values[0], $values[0])
	else if length($values) == 2
		push($newValues, $values[0], $values[1], $values[0], $values[1])
	else if length($values) == 3
		push($newValues, $values[0], $values[1], $values[2], $values[1])
	else if length($values) == 4
		push($newValues, $values[0], $values[1], $values[2], $values[3])
	return $newValues
